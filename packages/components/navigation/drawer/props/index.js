export const drawerProps = {
    modelValue: Boolean,
    altitude: {
        type: String,
        default: '600px'
    },
    right: Boolean,
    bottom: Boolean,
    top: Boolean,
    color: String,
    noHeader: Boolean
}