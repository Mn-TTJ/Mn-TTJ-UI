export const stepsProps = {
    stage: Number,
    center: Boolean,
    space: String,
    column: Boolean,
    icon: Boolean,
    simple: Boolean,
    doneColor: {
        type: String,
        default: '#409eff'
    },
    doingColor: {
        type: String,
        default: '#67c23a'
    },
    willDoColor: {
        type: String,
        default: '#a8abb2'
    }
}