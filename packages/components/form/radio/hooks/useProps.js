export default function () {
    let props = {
        labels: Array,
        group: String,
        modelValue: String,
        circular: Boolean,
        tick: Boolean,
        noBorder: Boolean,
        btn: Boolean,
        full: Boolean,
        custom: Boolean
    }
    return props
}