export default function () {
    let props = {
        modelValue: String,
        dataSet: Array,
        multiple: Boolean,
        uStyle: Object,
        shower: Boolean,
        panel: Boolean,
        lazy: Boolean,
        callBack: Function,
        disabled: Boolean,
    }
    return props
}