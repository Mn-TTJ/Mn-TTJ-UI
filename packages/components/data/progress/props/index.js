export const Props = {
    height: String,
    percentage: {
        type: Number,
        default: 0
    },
    color: {
        type: String,
        default: '#409eff'
    },
    status: String,
    inside: Boolean,
    circle: Boolean,
    dashboard: Boolean
}