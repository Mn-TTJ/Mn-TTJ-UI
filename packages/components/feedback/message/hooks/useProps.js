export default function () {
    return {
        showMessage: Boolean,
        text: String,
        type: String,
        offset: Number,
        onClose: Function,
        destroy:Function,
    }
}